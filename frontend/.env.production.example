# Required at build time. No trailing slash.
VITE_API_BASE_URL=https://api.example.com
# Keep false for every live deployment. true is only for an explicitly labelled demo build.
VITE_ENABLE_DEMO_MODE=false
